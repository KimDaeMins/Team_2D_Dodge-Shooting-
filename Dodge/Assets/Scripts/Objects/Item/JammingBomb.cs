using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JammingBomb : Object_Base
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("투사체를 국소적으로 파괴합니다.");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
